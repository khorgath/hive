
-- this test is going to fail , since it is more of a demo of acceptable syntax, rather than
-- actual repl loads from dirs. We should expand this out so that each of these will eventually
-- pass, due to an export having been done to support them.

REPL LOAD FROM '/tmp/foo';

REPL LOAD drrbrr1 FROM '/tmp/foo';

REPL LOAD drrbrr2.trrbrr2 FROM '/tmp/foo';

